export interface UserProfile {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}

export interface UserProfileToken {
    firstName: string;
    lastName: string;
    email: string;
    token: string;
}
